{
  "compilerOptions": {
    "target": "ESNext",                          // Suporte a recursos modernos do JS
    "module": "ESNext",                          // Usado com Vite (ESModules)
    "lib": ["DOM", "DOM.Iterable", "ESNext"],    // Suporte para APIs do navegador
    "allowJs": false,                            // Desabilita arquivos JS (só TS)
    "skipLibCheck": true,                        // Ignora checagem de tipos em node_modules
    "esModuleInterop": true,                     // Suporte para importações estilo ES
    "moduleResolution": "Node",                  // Resolve imports como no Node.js
    "resolveJsonModule": true,                   // Permite importar arquivos .json
    "isolatedModules": true,                     // Garante arquivos independentes (Vite exige)
    "noEmit": true,                              // Não gera arquivos JS na build
    "jsx": "react-jsx",                          // JSX moderno com React 17+
    "strict": true,                              // Ativa todas as verificações estritas
    "forceConsistentCasingInFileNames": true,    // Garante consistência entre sistemas
    "useDefineForClassFields": true              // Compatibilidade com novas sintaxes do TS
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
